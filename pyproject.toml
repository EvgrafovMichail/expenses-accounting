[project]
name = "money"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pandas>=3.0.0",
]

[dependency-groups]
dev = [
    "ipykernel>=7.1.0",
    "mypy==1.19.1",
    "pre-commit==4.5.1",
    "ruff==0.15.0",
]
